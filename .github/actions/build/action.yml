name: 'translationCore Build Environment'
description: 'Run the translationCore Build Environment'
author: 'unfoldingWord'
inputs:
  run:
    description: 'Commands to run'
    default: 'npm test'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.run }}
