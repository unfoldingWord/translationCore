#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../electron-mocha/bin/electron-mocha" "$@"
  ret=$?
else 
  node  "$basedir/../electron-mocha/bin/electron-mocha" "$@"
  ret=$?
fi
exit $ret
