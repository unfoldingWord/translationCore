// Created classes for each font in main css (styles.css) which include the font setting for each font.
const fonts = {
  'Padauk': { 'font': 'Padauk' },
  'Awami Nastaliq': { 'font': 'AwamiNastaliq' },
  'Scheherazade': { 'font': 'Scheherazade' },
  // Add more complex script fonts here, remember to load it in the css file and create a class with the font setting if necessary.
};

export default fonts;
