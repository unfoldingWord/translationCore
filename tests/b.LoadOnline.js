const assert = require('chai').assert;
const loadOnline = require('./src/components/core/LoadOnline.js');

describe('loadOnline.openManifest', function() {
  it('loadOnline.openManifest should download a remote repository via git, and return the save location.', function(done){
  });
})
